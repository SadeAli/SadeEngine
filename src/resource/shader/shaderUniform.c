#include "shader.h"
#include "shaderUniform.h"
#include "glad/glad.h"

void shaderUniform_apply(ShaderProgram shader, ShaderUniform uniform) {
    glUseProgram(shader);
    switch (uniform.type) {
        case 
    
    }
}
